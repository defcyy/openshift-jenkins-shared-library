@Library('openshift-jenkins-shared-library@master') _

projectPipeline {
  withEnvironment("dev") {
    withNode("master") {
      echo "${project} - ${namespace} - ${environment}"
    }

    withNode("maven") {
      echo "${project} - ${namespace} - ${environment}"
    }
  }
}